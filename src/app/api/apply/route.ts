// // app/api/apply/route.ts
import { NextResponse } from "next/server";

export async function POST(request: Request) {
  // This is a temporary stub. Remove or update this file when you're ready to implement email sending.
  return NextResponse.json({
    message: "API endpoint is temporarily disabled. It will be fixed later.",
  });
}
